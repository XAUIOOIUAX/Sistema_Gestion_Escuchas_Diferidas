# Sistema de Gestión de Escuchas Diferidas — dependencias de runtime
#
# Versiones fijadas: un ejecutable se compila una vez y se distribuye; que la
# próxima compilación traiga otra versión de Qt y cambie el comportamiento en
# los equipos de la fiscalía no es aceptable.
#
#     py -m pip install -r requirements.txt

PySide6==6.11.1          # interfaz de escritorio (incluye QtWebEngine)
openpyxl==3.1.5            # importación / exportación .xlsx
python-docx==1.2.0          # informes .docx

# --- Empaquetado ---
pyinstaller==6.11.1

# --- El motor de transcripción NO se instala acá ---
# Whisper y torch pesan gigas y corren en un intérprete APARTE, con el modelo y
# ffmpeg en `recursos/` al lado del ejecutable. Ver README y
# app/servicios/transcripcion.py. En el equipo que transcribe:
#     py -m pip install openai-whisper==20250625 faster-whisper==1.2.1
